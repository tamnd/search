---
title: "Reference"
linkTitle: "Reference"
description: "The complete sx surface: every command and flag, the Go options, the C ABI, and the release history."
weight: 30
featured: true
---

The exhaustive reference. The [CLI](/reference/cli/) page lists every `sx`
command and flag; the [configuration](/reference/configuration/) page covers the
Go `Options` and the on-disk file conventions; the [C ABI](/reference/c-abi/)
page documents `libsearch` and the `cabi/search.h` header; and the
[release notes](/reference/release-notes/) summarise each version.
